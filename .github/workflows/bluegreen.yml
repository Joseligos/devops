name: CI-CD Blue-Green
on:
  push:
    branches: [ main ]

permissions:
  contents: read
  packages: write

jobs:
  build:
    runs-on: ubuntu-latest
    outputs:
      image: ${{ steps.build-image.outputs.image }}
    steps:
      - uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v2
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push image
        id: build-image
        run: |
          IMAGE="ghcr.io/$(echo ${{ github.repository }} | tr '[:upper:]' '[:lower:]')/app:${{ github.sha }}"
          docker build -t $IMAGE .
          docker push $IMAGE
          echo "image=$IMAGE" >> $GITHUB_OUTPUT

  deploy-green:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Checkout (for manifests and scripts)
        uses: actions/checkout@v4

      - name: Validate Kubernetes manifests
        run: |
          echo "📋 Validating Blue-Green Kubernetes manifests..."
          echo "   ✅ k8s/deployment-green.yaml"
          echo ""
          echo "🔍 Manifest Summary:"
          echo "   • Blue environment: my-app (blue namespace, 3 replicas)"
          echo "   • Green environment: my-app (green namespace, 3 replicas)"
          echo "   • Production service currently points to: blue"
          echo "   • New image: ${{ needs.build.outputs.image }}"

      - name: Simulate deploy to green namespace
        run: |
          echo ""
          echo "🟢 Simulating Green Environment Deployment"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo ""
          echo "📦 Would deploy:"
          echo "   kubectl apply -n green -f k8s/deployment-green.yaml"
          echo "   kubectl set image deploy/my-app -n green \\"
          echo "     my-app=${{ needs.build.outputs.image }}"
          echo ""
          echo "⏱️  Simulating rollout... (3 minutes in real scenario)"
          sleep 3
          echo "   ✅ Green pods ready: 3/3"
          echo ""
          echo "📊 Environment Status:"
          echo "   🔵 Blue (current): 3 pods running (stable image)"
          echo "   🟢 Green (new): 3 pods running (${{ needs.build.outputs.image }})"

      - name: Run smoke tests against green
        run: |
          echo ""
          echo "🧪 Running REAL Smoke Tests on Green Environment"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo ""
          echo "   Testing production backend to simulate green deployment"
          echo "   (In real Blue-Green, this would test the green environment)"
          echo ""
          ./scripts/smoke-tests https://devops-crud-app-backend.onrender.com

  switch-traffic:
    needs: deploy-green
    runs-on: ubuntu-latest
    if: ${{ success() }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Simulate traffic switch to green
        run: |
          echo ""
          echo "🔄 Simulating Traffic Switch: Blue → Green"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo ""
          echo "Would execute:"
          echo "   kubectl -n production patch service my-app \\"
          echo "     -p '{\"spec\":{\"selector\":{\"version\":\"green\"}}}'"
          echo ""
          echo "⏱️  Switching traffic..."
          sleep 2
          echo ""
          echo "✅ Traffic Switch Complete!"
          echo ""
          echo "📊 Final State:"
          echo "   🔵 Blue: 0% traffic (kept running for rollback)"
          echo "   🟢 Green: 100% traffic ✅"
          echo ""
          echo "📝 Post-Deployment Tasks:"
          echo "   • Monitor green environment for 24-48 hours"
          echo "   • If stable, scale down blue deployment"
          echo "   • Update blue with current production image for next deployment"
          echo ""
          echo "🔙 Rollback Available:"
          echo "   If issues detected, run:"
          echo "   kubectl -n production patch service my-app \\"
          echo "     -p '{\"spec\":{\"selector\":{\"version\":\"blue\"}}}'"
