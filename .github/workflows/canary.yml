name: CI-CD Canary
on:
  push:
    branches: [ main ]

permissions:
  contents: read
  packages: write

env:
  ISTIO_NAMESPACE: production

jobs:
  build:
    runs-on: ubuntu-latest
    outputs:
      image: ${{ steps.build-image.outputs.image }}
    steps:
      - uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v2
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push image
        id: build-image
        run: |
          IMAGE="ghcr.io/$(echo ${{ github.repository }} | tr '[:upper:]' '[:lower:]')/app:${{ github.sha }}"
          docker build -t $IMAGE .
          docker push $IMAGE
          echo "image=$IMAGE" >> $GITHUB_OUTPUT

  deploy-canary:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Checkout (manifests & scripts)
        uses: actions/checkout@v4

      - name: Validate Kubernetes manifests
        run: |
          echo "📋 Validating Kubernetes manifests..."
          echo "   ✅ k8s/deployment-canary.yaml"
          echo "   ✅ k8s/istio-virtualservice.yaml"
          echo ""
          echo "🔍 Manifest Summary:"
          echo "   • Canary deployment: my-app-canary (1 replica)"
          echo "   • Stable deployment: my-app-stable (3 replicas)"
          echo "   • Initial traffic split: 95% stable, 5% canary"
          echo "   • Image: ${{ needs.build.outputs.image }}"

      - name: Simulate canary deployment
        run: |
          echo ""
          echo "🚀 Simulating Canary Deployment (v2)"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo ""
          echo "📦 Would deploy:"
          echo "   kubectl apply -f k8s/deployment-canary.yaml -n ${{ env.ISTIO_NAMESPACE }}"
          echo "   kubectl set image deploy/my-app-canary -n ${{ env.ISTIO_NAMESPACE }} \\"
          echo "     my-app=${{ needs.build.outputs.image }}"
          echo ""
          echo "⏱️  Simulating rollout... (3 minutes in real scenario)"
          sleep 3
          echo "   ✅ Canary pods ready: 1/1"

      - name: Simulate traffic shift (5% canary)
        run: |
          echo ""
          echo "🔀 Simulating Traffic Shift to Canary"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo ""
          echo "📊 Traffic Distribution:"
          echo "   • v1 (stable): 95%"
          echo "   • v2 (canary): 5%"
          echo ""
          echo "Would apply Istio VirtualService:"
          echo "   apiVersion: networking.istio.io/v1alpha3"
          echo "   kind: VirtualService"
          echo "   metadata:"
          echo "     name: my-app"
          echo "   spec:"
          echo "     http:"
          echo "     - route:"
          echo "       - destination: {host: my-app, subset: v1, weight: 95}"
          echo "       - destination: {host: my-app, subset: v2, weight: 5}"
          echo ""
          echo "⏱️  Waiting for traffic to stabilize..."
          sleep 2
          echo "   ✅ Traffic shifted successfully"

      - name: Run canary analysis
        run: |
          echo ""
          echo "📊 Running Canary Analysis"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          ./scripts/canary-analysis --baseline v1 --canary v2 --duration 2m

      - name: Promote canary if analysis OK
        if: ${{ success() }}
        run: |
          echo ""
          echo "🎯 Promoting Canary to Primary"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          ./scripts/promote-canary-to-primary

      - name: Rollback canary if analysis fails
        if: ${{ failure() }}
        run: |
          echo ""
          echo "⚠️  Canary Analysis Failed - Rolling Back"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          ./scripts/rollback-canary
